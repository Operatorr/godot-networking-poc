[package]
name = "sim_core"
version.workspace = true
edition.workspace = true
description = "Omega Realm shared simulation core — movement, collision, hit predicates (migration-spec D5)"

[dependencies]
